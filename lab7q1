#include <stdio.h>

// Declare the global environ variable
extern char **environ;

int main()
{
   int i=0;

    while (environ[i]!=NULL) {
        printf("%s\n", environ[i]);
        i++;
    }

    return 0;
}
